# Factory Design Pattern
This pattern aims at creating a one-door factory which could create various
type of objects using (key, value) objects. We will supply the object name 
and related source of that object.  
It also involves Cache mechanism which is similar to singleton idea